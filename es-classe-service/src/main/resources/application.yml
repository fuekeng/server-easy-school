server:
  port: 9003
spring:
  application:
    name: es-courses services
  profiles:
    active: dev
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:9000/eureka
